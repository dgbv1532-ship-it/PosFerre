# Railway deployment configuration
# Each service is deployed from its directory

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm --filter api db:migrate:prod && pnpm --filter api start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
